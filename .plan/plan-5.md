# Task: Integrate API Documentation using dedoc/scramble.

## Instructions:

1. Install dedoc/scramble via Composer.

2. Publish the Scramble configuration.

3. Configure Scramble to only parse routes starting with api/.

4. Review the previously created Controllers (Auth, Wallet, Category, Transaction, Transfer). Add descriptive PHPDoc blocks to any Controller methods where Scramble might need help inferring the response, especially for the optional photo upload in Transactions.

5. Ensure the API documentation clearly shows the required Authorization: Bearer {token} header for all endpoints.
